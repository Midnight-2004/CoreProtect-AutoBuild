name: Build and Deploy

on:
  schedule:
    - cron: '0 5 * * 0'  # 仅周日凌晨5点触发
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 发布所需的权限

    steps:

      # 克隆上游仓库（保持与上游同步的关键修改）
      - name: Clone Upstream Repository
        run: git clone --branch master https://github.com/PlayPro/CoreProtect ./external-coreprotect

      # 修改上游的 pom.xml
      - name: Patch pom.xml
        working-directory: ./external-coreprotect
        run: |
          # 修改版本号和分支参数
          sed -i '5s|<version>[^<]*</version>|<version>24.0.2-dev</version>|' pom.xml
          sed -i '7s|<project.branch></project.branch>|<project.branch>development</project.branch>|' pom.xml
          cat pom.xml  # 验证修改

      # 设置 Java 环境
      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'temurin'

      # Maven 依赖缓存（路径与上游统一）
      - name: Cache Maven packages
        uses: actions/cache@v5
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('external-coreprotect/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2-

      # Maven 构建
      - name: Build with Maven
        run: mvn -B verify
        working-directory: ./external-coreprotect

      # 构建后的验证步骤（精确检查两个文件）
      - name: Verify Artifacts
        run: |
          ls -la ./external-coreprotect/target/
          
          # 检查核心文件是否存在（泛匹配 CoreProtect-x.x.jar）
          if ! ls ./external-coreprotect/target/CoreProtect-*.jar; then
            echo "::error::CoreProtect-*.jar not found"
            exit 1
          fi

          echo "Required JAR files exist"

      - name: Get current time
        id: time
        run: echo "timestamp=$(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_OUTPUT

      - name: Create or Update Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest-build
          name: "Latest Build"
          body: "CoreProtect开发版本的自动构建。\nAutomated build of CoreProtect development version.\n\nLast updated: ${{ steps.time.outputs.timestamp }}\nBuild triggered by: ${{ github.event_name }}"
          files: "./external-coreprotect/target/*.jar"
          draft: false
          prerelease: false
          overwrite_files: true
          append_body: false
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
